# 미리봄 v2 — 표지·썸네일·대시보드 (작업 계획·기준)

원본 기획서(사용자 제공)를 이 저장소 작업 기준으로 통합. 확정 결정 반영.

## 확정 결정 (2026-06-21)
- **보안: 가볍게 간다.** `covers` 버킷 = **공개 읽기(이미지)** + **작성자만 쓰기/삭제**(Storage RLS). 원고 **텍스트는 기존대로 비공개**(RLS·공유토큰). → 표지는 대시보드·뷰어·공유 어디서든 **공개 URL로 바로** 표시(서명URL·Edge Function 미사용). 표지 이미지는 URL 알면 접근 가능하나 경로가 임의 uuid라 사실상 비공개로 취급.
- **자동표지 색**: `hash(id) % 8` (DB 컬럼 없이 결정론적). 같은 책=항상 같은 색.
- 8색 팔레트 HEX·리사이즈 1600/500px·4열·카드클릭=뷰어 = 기획서 기본값(구현 중 미세조정 가능).

## 단계
- **A (프론트, 백엔드 무관)**: 대시보드 표시영역 1080px·가운데정렬, 3:4 세로 카드 4열, **표지없는 자동 썸네일**(8색 결정론 배경 + 가는 선 + 세리프 제목 + 저자, "긴 여름의 끝" 스타일), **롤오버 오버레이 4버튼**(뷰어/썸네일넣기/공유/삭제) + 터치 `⋯` 대체. 기존 상시 삭제버튼(.card-x) 대체. (썸네일넣기는 C단계까지 스텁)
- **B (백엔드)**: 마이그레이션6 — `documents`에 `cover_path`·`cover_thumb_path`(text, nullable) 추가, `covers` 공개읽기 버킷(2MB·jpg/png/webp, 작성자만 쓰기) + Storage RLS. 가벼운 격리 확인(작성자만 업로드/삭제). 사용자 SQL 적용.
- **C (업로드)**: "썸네일 넣기" → 브라우저 리사이즈(표시 ~1600px / 썸네일 ~500px) 2장 업로드 → cover_path/cover_thumb_path 저장. **이미지 썸네일**(object-fit:cover 크롭 + 하단 그라데이션 스크림 + 흰 제목 HTML 오버레이). 교체/삭제 시 Storage 객체까지 제거. 원고 삭제 시 표지 객체도 제거.
- **D (뷰어·공유·정리)**: 뷰어 표지 페이지 **전면화**(여백0·inset:0·시트 라운드에 맞춰 클립). 공유 스냅샷에 표지 표시(공개 URL). 본문 인라인 md 이미지(`![](경로)`)는 **로드 안 하고 플레이스홀더**(파서는 유지, 로드만 차단). 설명서에서 "마크다운 경로 표지" 방법 제거.

## 수용 기준
기획서 §9 기준. 단 표지 접근은 "공개 읽기 / 작성자만 쓰기"로 완화(중자격 테스트는 가볍게).

## 데이터 모델 추가 (B단계)
- `documents.cover_path` text null, `documents.cover_thumb_path` text null.
- Storage 버킷 `covers`: public read, owner-only write/delete, 2MB, image/jpeg·png·webp.

## 진행 메모
- (A) **완료**(2026-06-21): 대시보드 1080·3:4·4열(데탑)/3열(≤900)/2열(≤560). 자동 썸네일 = 깊은 8색(id해시) + 가는 크림선 + 세리프 제목(4줄 말줄임) + 저자(content_md에서 추출). 롤오버 2×2 4버튼(뷰어/표지/공유/삭제) + 터치 `⋯`. 카드 `<button>`→`<div role=button>`로 바꿔 버튼 중첩 해소. 다운로드는 롤오버 5번째 버튼(가로 전체폭, 4버튼 아래)으로 복원(사용자 요청). 표지넣기는 C단계 스텁(alert). 로컬: 치수·색·구조 검증 / 호버 오버레이 출현·터치 ⋯·실데이터 렌더는 실기기/로그인 확인 필요.
- (B) **완료**(2026-06-21): 마이그레이션6 작성·커밋 + 사용자 SQL Editor 적용 완료(Success). 표지 컬럼 + covers 공개읽기 버킷 + 작성자만 쓰기 RLS.
- (C) **완료**: 표지 업로드(브라우저 리사이즈 1600/500 → covers/{uid}/{docid}/ 업로드 → cover_path·cover_thumb_path 저장), 이미지 썸네일(크롭+그라데이션+흰 제목), 표지 교체/제거(Storage 객체까지), 원고 삭제 시 표지 정리. 원본 상한 15MB(리사이즈로 저장본은 수~수십KB). loadDocs select에 표지 컬럼 추가. **실업로드 동작은 로그인 필요(배포 후 확인).**
- (디자인 수정 2026-06-21): 표시영역 1080→**900**, 카드 **테두리 제거**, **업로드일시**(자동=하단중앙 / 이미지=제목밑, 고딕 "오늘 HH시 MM분"), **우상단 원고지매수 배지**(레이어 아이콘 + 숫자, 200자=1매, 프론트매터 제외·공백압축 후 환산), `⋯`는 좌상단으로 이동.
- (D) 대기: 뷰어 표지 전면화 + 공유 표지 표시 + 본문 인라인 이미지 플레이스홀더 + 설명서.

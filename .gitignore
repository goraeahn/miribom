# ── 비밀/환경값 (절대 커밋 금지) ───────────────────────────────
.env
.env.local
.env.*.local
# config.js 는 anon(publishable) key 만 담으므로 커밋 허용

# ── 민감 콘텐츠 (미발표 창작 원고 — 공개 금지) ─────────────────
메아리_검증픽스처.md

# ── Supabase CLI 로컬 산출물 ───────────────────────────────────
.branches/
.temp/
supabase/.temp/

# ── OS / 에디터 / 도구 ─────────────────────────────────────────
.DS_Store
Thumbs.db
node_modules/
.claude/
